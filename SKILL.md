---
name: bel-hot-sales
description: >-
  From a 十部半托管-库存管理表 (.xlsx) workbook, produce the "两周均有销量" hot-selling
  product table in the established V3 layout: filter MSKU brand segment, aggregate by
  款号, split last week into 7 daily columns, summarize the week before, and add a
  week-over-week 环比 column. Use this skill whenever the user sends a
  十部半托管 inventory/sales workbook and asks for 热销/销量 products, "筛选出有销量的产品",
  "按 V3 表输出", BEL/BH/TH/BLA brand sales rankings, two-week comparisons, or any
  request to reproduce the BEL两周有销量 / BEL销量Top15 style table. Also use it when
  they say "按之前的要求" or "按之前的口径" while attaching such a workbook, even if they
  do not name the skill.
---

# BEL 热销销量表（V3 格式）

把「十部半托管-库存管理表」转成两周均有销量的热销产品表。

## 何时用

用户发来一个十部半托管库存表（或任何含 `总销售表` sheet 的销量表），并且想要：

- 筛选出有销量的产品 / 热销产品
- 两周销量对比、环比
- 「按 V3 表输出」「按之前的要求/口径输出」
- 某个品牌（BEL / BH / TH / BLA …）的销量排名

## 核心口径（不要自行改动）

这几条是用户明确确认过的业务规则；口径变了结论就变了，所以除非用户当场改口，一律照做：

| 项目 | 规则 |
|---|---|
| 数据源 | `总销售表` sheet |
| 品牌筛选 | MSKU **第 2 段**（按 `-` 切分）= 品牌，如 `TM-BEL-102503-5SetA-S` → `BEL` |
| 聚合粒度 | **款号** |
| 日期窗口 | 最后一个**有数据的**日期列为 D0；上周 = D0 前 7 天，上上周 = 再前 7 天 |
| 保留条件 | 上周合计 > 0 **且** 上上周合计 > 0 |
| 排序 | 上周合计 ↓ → 上上周合计 ↓ → 款号 |
| 输出格式 | 固定 V3（上周拆 7 天，上上周汇总一列，带环比） |

### ⚠️ 两个必须知道的坑

**1. MSKU 不是以品牌开头的。** 格式是 `平台-品牌-款号-颜色-尺码`。用户口语说的「BEL 前缀」实际指品牌段。如果按 `startswith("BEL")` 过滤会得到 **0 行**。这个坑之前真实踩过，务必按段匹配。

**2. 表头有重复日期标签。** 早期列里 `4.24日` 出现过两次。所以**不能用 (月,日) 做字典 key**——会互相覆盖导致窗口偏移（曾把 9.18 误算成 5.25）。一律按**列位置**处理。脚本已经处理好了，别自己另写一套。

## 用法

```bash
python scripts/bel_pipeline.py "<十部半托管-库存管理表.xlsx>" [输出路径] [--brand BEL]
```

脚本会打印探测到的 D0 与两个窗口，便于核对：

```
来源 sheet: 总销售表  数据行: 11488
D0(最后有数据日) = 9.18日
上周窗口   = 9.12 .. 9.18
上上周窗口 = 9.5 .. 9.11
BEL 行数=1177  BEL 款号=114  保留=35  排除=11
已保存: ...
```

**务必核对打印出的窗口日期**是否符合用户对「这两周」的理解。如果用户说的两周与脚本探测的不一致（例如他要的是 9.01–9.07 和 9.08–9.14），用 `--last-week` / `--prev-week` 显式指定，别猜。

## 输出结构

两个 sheet：

1. **两周均有销量** — A1:M，13 列
   `排名 | 款号 | 上上周销量合计 | 上周每日×7 | 上周销量合计 | 最近7天日均 | 上周环比上上周`
   - 橙色横幅「上周每日销量（9.12 - 9.18）」覆盖 7 个日列
   - 前三名行米黄底 `FFF2CC`，日销单元格浅橙 `FCE4D6`
   - 环比 `+0.0%` 格式，正红 `C00000` / 负蓝 `0070C0` / 零灰 `808080`
   - 冻结 `C5`（向右滚动时排名与款号固定）
2. **已排除-仅单周有销量** — 被两周交集排除的款号及原因

## 交付时要说明什么

用户看重「哪些没进来」。交付时除了表本身，**主动提示被排除的重点款**：

- 仅上周有销量 → 新起量
- 仅上上周有销量 → 已断货

这些款常常是用户真正关心的（例如曾有一款上周卖 10 件、是当周第 9 大，但因上上周为 0 被排除，必须点出来，否则用户会以为漏了）。

同时提醒**小基数导致的环比失真**：如某款 2 → 22 件显示 +1000%，严格合规但几乎无参考意义。如果用户要的是「稳定在售」而非「两周沾边」，建议加销量门槛。

## 校验习惯

改完或生成后，用脚本自带的校验确认 **7 个日列之和 = 上周合计**（每一行）。这个不变量能同时抓住窗口错位和数据聚合错误，是性价比最高的检查。

## 输出命名

默认写到 `BEL两周有销量_输出.xlsx`（多品牌时用品牌名）。如果目标文件正被 Excel 打开（存在 `~$` 锁文件、写入报 PermissionError），**不要强行覆盖**——另存一个带 `_v2` 后缀的新文件并告诉用户，避免破坏他正在看的内容。
